<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.android.labmanager">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/scientist"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/scientist"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.activity_query.QueryActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.activity_property_card.PropertyCardActivity"
            android:label="Compound Card"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".ui.activity_list.CompoundsListActivity"
            android:label="List"
            android:theme="@style/AppTheme.NoActionBar" />

        <activity
            android:name=".ui.activity_backup.BackupActivity"
            android:label="Backup"
            android:theme="@style/AppTheme.NoActionBar" />

    </application>

</manifest>